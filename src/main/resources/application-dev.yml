server:
  port: 8080

spring:
  application:
    name: leitao-angola

  datasource:
    url: jdbc:postgresql://localhost:5433/lelo_angola
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

jwt:
  secret: "leloAngolaSecretKey1234567890ForDevelopmentOnly"
  expiration: 86400000
  refresh-expiration: 604800000

# Origens permitidas para CORS e handshake WebSocket (dev: lelo-web Vite + lelo-mobile Expo).
# Restringir ao domínio real do frontend em produção (SPEC §5.6).
cors:
  allowed-origins: http://localhost:5173,http://localhost:4173,http://localhost:19006,http://localhost:8081
